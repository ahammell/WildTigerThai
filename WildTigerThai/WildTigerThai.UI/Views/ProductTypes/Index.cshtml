@model IEnumerable<WildTigerThai.DATA.EF.ProductType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Active)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductType_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductType_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductType_ID })
        </td>
    </tr>
}

</table>
