@model IEnumerable<WildTigerThai.DATA.EF.ProductsToSection>

@{
    ViewBag.Title = "Products In Sections";
}

<h2 class="center">@ViewBag.Title</h2>
<p>
    @Html.ActionLink("Admin Panel", "AdminDashboard", "Home", null, new { @class = "btn btn-warning", @role = "button", @style = "width:8em; float:left;" })
</p>
<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-warning", @role = "button", @style = "width:8em; clear:right;" })
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Product.ProductName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Section.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Product.ProductName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Section.Name)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.ProductToSection_ID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.ProductToSection_ID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.ProductToSection_ID })
                </td>
            </tr>
        }
    </tbody>
</table>
